{
  "name": "community-song-v2",
  "repository": {
    "type": "git",
    "url": "https://github.com/scholtz-gnome/community-song-v2"
  },
  "version": "0.1.0",
  "engine": {
    "node": "14.16.1",
    "npm": "6.14.12"
  },
  "scripts": {
    "app:serve": "concurrently 'cd backend && npm run dev' 'cd frontend && npm start'",
    "frontend:test": "concurrently 'cd backend && npm run dev' 'cd frontend npm start' 'cd frontend && npx cypress run --browser chrome'",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
